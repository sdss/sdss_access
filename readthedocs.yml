python:
  version: 3.7
  install:
    - requirements: requirements_doc.txt
    - method: pip
      path: requirements_doc.txt 
    - method: setuptools
      path: package

sphinx:
  configuration: docs/sphinx/conf.py

build:
  image: latest